package com.google.jenkins.plugins.credentials.oauth.thirdparty;

import com.google.jenkins.plugins.credentials.oauth.OAuth2ScopeRequirement;

import org.apache.oltu.oauth2.common.OAuthProviderType;

import java.util.Collection;

/**
 * TODO: Insert description here. (generated by elibixby)
 */
public class OAuth3LOScopeRequirement extends OAuth2ScopeRequirement {

  private final Collection<String> scopes;
  private final OAuthProviderType provider;
  
  public OAuth3LOScopeRequirement(Collection<String> scopes, OAuthProviderType provider) {
    this.scopes = scopes;
    this.provider = provider;
  }

  /* (non-Javadoc)
   * @see com.google.jenkins.plugins.credentials.oauth.OAuth2ScopeRequirement#getScopes()
   */
  @Override
  public Collection<String> getScopes() {
    return scopes;
  }
  
  public OAuthProviderType getProvider(){
    return provider;
  }

}
